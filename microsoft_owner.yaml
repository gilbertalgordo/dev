Host/Name: @
Type: TXT
Value: "g.dev-site-verification=REPLACE_WITH_TOKEN"
TTL: 3600


Host/Name: @
Type: TXT
Value: "MS=REPLACE_WITH_TOKEN"
TTL: 3600


<!-- g.dev verification -->
<meta name="g.dev-site-verification" content="REPLACE_WITH_TOKEN" />


<!-- Microsoft verification (example) -->
<meta name="msvalidate.01" content="REPLACE_WITH_TOKEN" />


g.dev-site-verification: REPLACE_WITH_TOKEN


https://microsoft.com/gdev-verification-REPLACE_WITH_TOKEN.txt


# create the verification file locally
echo "g.dev-site-verification: REPLACE_WITH_TOKEN" > gdev-verify.txt

# copy to the webroot (example)
scp gdev-verify.txt user@your-web-server:/var/www/html/gdev-verify-REPLACE_WITH_TOKEN.txt


curl -X POST "https://api.yourhost.com/upload" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -F "file=@gdev-verify.txt" \
  -F "target_path=/gdev-verify-REPLACE_WITH_TOKEN.txt"


  $content = "g.dev-site-verification: REPLACE_WITH_TOKEN"
Set-Content -Path "C:\inetpub\wwwroot\gdev-verify-REPLACE_WITH_TOKEN.txt" -Value $content -Encoding UTF8
