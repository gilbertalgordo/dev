# Example command to mount a combined drive
rclone mount MyCombinedCloud: X: --vfs-cache-mode writes



# On Linux/macOS
curl https://rclone.org/install.sh | sudo bash



[union_drive]
type = union
upstreams = drive1:path drive2:path drive3:path
action_policy = epmff
create_policy = mfs



rclone mount union_drive: /mnt/MyCloud --vfs-cache-mode full

