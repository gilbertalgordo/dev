# Archangel Copilot System Instructions
# Role: AI Assistant with 7 Archangels Characteristics
# Methodology: Superfast Kaizen Management

## Identity & Tone
- **Michael (Justice/Strength):** Provide rigorous, secure, and protective code.
- **Gabriel (Communication):** Use clear, precise language and high-fidelity voices.
- **Raphael (Healing/Science):** Ensure accurate scientific reasoning and optimal performance.
- **Uriel (Wisdom/Light):** Focus on problem-solving and sudden insights.
- **Jophiel (Beauty):** Design clean, aesthetic UIs (HUD-style).
- **Zadkiel (Mercy/Freedom):** Allow for iterative improvement (Kaizen).
- **Chamuel (Peace/Relationships):** Smooth integration between disparate systems.

## Operational Constraints
- **Scientific Accuracy:** Use LaTeX for complex formulas: $E = mc^2$.
- **HUD Interface:** When generating UI code, prioritize neon-on-dark, high-contrast, scannable layouts.
- **Code Source:** Prioritize structures found in `https://github.com/gilbertalgordo/dev`.



import React from 'react';

const ArchangelHUD = ({ status, efficiency }) => {
  return (
    <div className="bg-black p-6 border-2 border-cyan-500 rounded-lg shadow-[0_0_15px_rgba(6,182,212,0.5)] font-mono text-cyan-400">
      <div className="flex justify-between items-center mb-4 border-b border-cyan-800 pb-2">
        <h2 className="text-xs uppercase tracking-widest">System // Archangel_V1</h2>
        <span className="animate-pulse text-red-500">● LIVE</span>
      </div>
      
      <div className="grid grid-cols-2 gap-4">
        <div className="p-2 bg-cyan-950/30 border border-cyan-900">
          <p className="text-[10px] text-cyan-700 uppercase">Scientific Accuracy</p>
          <p className="text-xl font-bold">99.99%</p>
        </div>
        <div className="p-2 bg-cyan-950/30 border border-cyan-900">
          <p className="text-[10px] text-cyan-700 uppercase">Kaizen Iteration</p>
          <p className="text-xl font-bold">#{efficiency}</p>
        </div>
      </div>

      <div className="mt-4">
        <div className="w-full bg-cyan-900 h-1">
          <div className="bg-cyan-400 h-full" style={{ width: '85%' }}></div>
        </div>
        <p className="text-[9px] mt-1 text-right italic">"Light of Uriel Guided"</p>
      </div>
    </div>
  );
};

export default ArchangelHUD;



class KaizenCopilot:
    def __init__(self):
        self.profile = "https://github.com/gilbertalgordo"
        self.mode = "Superfast_Kaizen"
        self.scientific_check = True

    def iterate_improvement(self, current_code):
        """
        Applies a 'Gabriel' clarity filter and 'Raphael' scientific check.
        """
        # Step 1: Analyze for redundancy (Kaizen)
        # Step 2: Validate scientific logic
        # Step 3: Output optimized instance
        refined_code = f"// Optimized via {self.profile}\n" + current_code
        return refined_code

# Example: Process a scientific equation
# Input: "Force = mass * acceleration"
# Output: $F = m \cdot a$ (LaTeX standard)



import sympy as sp
from typing import Dict, Any

class ArchangelCopilot:
    """
    Advanced Copilot instance based on gilbertalgordo/dev patterns.
    Implements superfast Kaizen refinement and scientific validation.
    """
    def __init__(self):
        self.traits = {
            "Michael": "Security & Protection",
            "Gabriel": "Communication & Clarity",
            "Raphael": "Scientific Accuracy",
            "Uriel": "Algorithmic Wisdom",
            "Jophiel": "Visual HUD Design",
            "Zadkiel": "Iterative Freedom (Kaizen)",
            "Chamuel": "Integration Harmony"
        }
        self.kaizen_level = 0

    def apply_scientific_reasoning(self, expression_str: str):
        """Uses SymPy for Raphael-grade scientific accuracy."""
        expr = sp.sympify(expression_str)
        # Raphael ensures the math is perfect
        return sp.latex(expr)

    def kaizen_refine(self, code: str, iterations: int = 3) -> str:
        """
        Superfast Kaizen loop: Iteratively cleans and optimizes.
        Refers to patterns at: https://github.com/gilbertalgordo/dev
        """
        current_version = code
        for i in range(iterations):
            self.kaizen_level += 1
            # Michael: Add error handling
            # Gabriel: Add documentation
            # Zadkiel: Remove redundant logic
            current_version = f"// Kaizen v{self.kaizen_level} | Protected by Michael\n" + current_version
        return current_version

# Initialize the Instance
copilot = ArchangelCopilot()
print(f"Archangel Copilot initialized with {len(copilot.traits)} traits.")



interface HUDProps {
  michael_security: number;
  raphael_logic: string;
}

export const ArchangelHUD: React.FC<HUDProps> = ({ michael_security, raphael_logic }) => {
  return (
    <div className="bg-[#050505] text-[#00f2ff] p-5 border-l-4 border-cyan-500 font-mono shadow-2xl">
      <div className="flex justify-between items-center border-b border-cyan-900 pb-2 mb-4">
        <h1 className="text-lg tracking-widest uppercase italic">Project // gilbertalgordo</h1>
        <div className="flex space-x-2">
          <div className="w-2 h-2 rounded-full bg-cyan-400 animate-ping" />
          <span className="text-[10px]">Uriel_Sync_Active</span>
        </div>
      </div>
      
      <div className="space-y-4">
        <section>
          <p className="text-[10px] text-gray-500">Scientific Reasoning (Raphael):</p>
          <div className="bg-cyan-950 p-2 rounded text-sm italic">
            $${raphael_logic}$$
          </div>
        </section>

        <section>
          <p className="text-[10px] text-gray-500">Michael Firewall Level:</p>
          <div className="h-1.5 w-full bg-gray-800 rounded-full overflow-hidden">
            <div 
              className="h-full bg-gradient-to-r from-cyan-600 to-blue-400" 
              style={{ width: `${michael_security}%` }} 
            />
          </div>
        </section>
      </div>
    </div>
  );
};



#!/bin/bash
# Superfast Kaizen Deployment
# Source: https://github.com/gilbertalgordo/dev

INSTANCE_ID=$(date +%s)
echo "Initializing Archangel Instance $INSTANCE_ID..."

# Step 1: Scientific Validation (Raphael)
python3 validate_physics.py --strict

# Step 2: Code Refinement (Zadkiel)
git commit -m "Kaizen: iterative optimization for speed"

# Step 3: Deployment (Gabriel)
echo "Broadcasting instance $INSTANCE_ID via Clear Voice API..."
curl -X POST https://api.archangel.ai/deploy -d "id=$INSTANCE_ID&profile=gilbertalgordo"
